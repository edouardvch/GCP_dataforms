config {
    type: "table",
    schema: "dynamic_events_ip",
    name: "line_breaking_passes",
    description: "Dynamic events in possession: line breaking passes",
    bigquery: {
        clusterBy: ["match_id", "player_id"]
    },
    assertions:{
        nonNull: ["match_id", "player_id"]
    }
}

SELECT * FROM ${ref("skillcorner_external_data", "line_breaking_passes")}
